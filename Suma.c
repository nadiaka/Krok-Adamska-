// Dodawanie dwóch liczb w C

#include <stdio.h>

int main() {
   int x;
   scanf("%d", &x);
   int y;
   scanf("%d", &y);
   int suma;
   // SUMA
   suma = x+y;
   printf("%d + %d = %d", x, y, suma);
}
